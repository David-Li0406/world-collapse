name: env-diag

# Diagnose the main-env slowdown after the machine reset: torch CUDA, GPU vs CPU
# compute, package versions (missing?), and push-v3 per-step render timing.

on:
  workflow_dispatch:
    inputs:
      gpu:
        description: "GPU id"
        required: false
        default: "0"
      python_version:
        description: "Pin uv to this Python (e.g. 3.11 = pre-reset known-good). Empty = uv default."
        required: false
        default: ""
      mujoco_version:
        description: "Override mujoco version after uv sync (e.g. 3.1.6) to test render-path regression. Empty = pinned 3.3.0."
        required: false
        default: ""

jobs:
  run:
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 30
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup uv environment
        run: |
          export UV_CACHE_DIR="$HOME/.cache/uv"
          export PATH="$HOME/.local/bin:$HOME/.cargo/bin:$HOME/bin:$PATH"
          if ! command -v uv >/dev/null 2>&1; then
            curl -LsSf https://astral.sh/uv/install.sh | sh
            export PATH="$HOME/.local/bin:$PATH"
          fi
          uv --version
          PYARG=""
          if [ -n "${{ inputs.python_version }}" ]; then PYARG="--python ${{ inputs.python_version }}"; fi
          uv sync --frozen $PYARG
          if [ -n "${{ inputs.mujoco_version }}" ]; then
            echo "[diag] overriding mujoco -> ${{ inputs.mujoco_version }}"
            # Target the project venv EXPLICITLY (--python .venv/...): otherwise
            # uv pip install doesn't land in .venv. --no-deps+--reinstall bypass
            # the editable project's mujoco==3.3.0 pin.
            VENV_PY="$PWD/.venv/bin/python"
            uv pip install --python "$VENV_PY" --no-deps --reinstall "mujoco==${{ inputs.mujoco_version }}"
          fi
          # Use the venv python DIRECTLY (not 'uv run', which re-syncs to uv.lock
          # and would revert the mujoco override above).
          echo "[diag] venv python: $(.venv/bin/python -c 'import sys;print(sys.version)')"
          echo "[diag] mujoco: $(.venv/bin/python -c 'import mujoco;print(mujoco.__version__)')"

      - name: GPU state (util / power / clocks)
        run: |
          echo "=== per-GPU util/power (valid fields) ==="
          nvidia-smi --query-gpu=index,name,utilization.gpu,memory.used,power.draw,power.limit,clocks.current.sm,clocks_event_reasons.active --format=csv 2>&1 | head -12 || true
          echo "=== plain nvidia-smi (fallback) ==="
          nvidia-smi 2>&1 | head -25 || true

      - name: EGL per-device render benchmark
        shell: bash -eo pipefail {0}
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          export HF_HOME="$HOME/hf_cache"
          export MUJOCO_GL=egl
          export PYOPENGL_PLATFORM=egl
          NVIDIA_PREFIX=$(ls -d /opt/nvidia-* 2>/dev/null | head -1)
          if [ -n "$NVIDIA_PREFIX" ]; then
            export LD_LIBRARY_PATH="${NVIDIA_PREFIX}/lib64:/usr/lib64:${LD_LIBRARY_PATH:-}"
            export __EGL_VENDOR_LIBRARY_FILENAMES="${NVIDIA_PREFIX}/egl_vendor.d/10_nvidia.json"
            unset __EGL_VENDOR_LIBRARY_DIRS
          fi
          export PYTHONUNBUFFERED=1
          export PYTHONPATH="src:iVideoGPT/mbrl:iVideoGPT:${PYTHONPATH:-}"
          # NOTE: no CUDA_VISIBLE_DEVICES restriction here, so the probe can see
          # all EGL devices and benchmark each physical GPU's render speed.
          PYARG=""
          if [ -n "${{ inputs.python_version }}" ]; then PYARG="--python ${{ inputs.python_version }}"; fi
          .venv/bin/python -u scripts/egl_device_bench.py || true

      - name: Run diagnostic
        shell: bash -eo pipefail {0}
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          export HF_HOME="$HOME/hf_cache"
          export MUJOCO_GL=egl
          export PYOPENGL_PLATFORM=egl
          NVIDIA_PREFIX=$(ls -d /opt/nvidia-* 2>/dev/null | head -1)
          if [ -n "$NVIDIA_PREFIX" ]; then
            export LD_LIBRARY_PATH="${NVIDIA_PREFIX}/lib64:/usr/lib64:${LD_LIBRARY_PATH:-}"
            export __EGL_VENDOR_LIBRARY_FILENAMES="${NVIDIA_PREFIX}/egl_vendor.d/10_nvidia.json"
            unset __EGL_VENDOR_LIBRARY_DIRS
          fi
          export PYTHONUNBUFFERED=1
          export PYTHONPATH="src:iVideoGPT/mbrl:iVideoGPT:${PYTHONPATH:-}"
          export IVIDEOGPT_ROOT="$PWD/iVideoGPT"
          PYARG=""
          if [ -n "${{ inputs.python_version }}" ]; then PYARG="--python ${{ inputs.python_version }}"; fi
          CUDA_VISIBLE_DEVICES="${{ inputs.gpu }}" MUJOCO_EGL_DEVICE_ID=0 \
            .venv/bin/python -u scripts/main_env_diag.py
